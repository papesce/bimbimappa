---
"family-fun-map": patch
---

Mobile header refinements: the places summary now sits in the top row with the brand and list button, and the search bar lives in the second row where it collapses into a search icon by default and expands only while typing (autofocusing and collapsing again once you clear and blur). When the search is closed, the filter chips right-align and use the full second-row width. The mobile "Back to area" action was also unified with desktop behavior: it now refits the map to the area and closes the bottom sheet without opening the list panel.
