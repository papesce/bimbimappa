# Changesets

This directory contains changeset files created by `npx changeset`. These are consumed by CI to bump the version in `package.json` on each release.

Do not edit files in this directory manually.
