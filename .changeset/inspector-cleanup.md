---
"family-fun-map": minor
---

Inspector cleanup: consolidate place detail/action surfaces into a single hierarchy. Desktop map popups now show the essential info plus Nearby and Edit actions, with navigation links, source links, and Remove tucked behind a More toggle. The mobile bottom sheet peek shows name, address, primary link, and Edit, while the expanded sheet hosts the full details plus the shared action row. Replaced the Explore icon (Radar) with a Compass glyph and added a delete-only inline confirm for the shared action surface.
