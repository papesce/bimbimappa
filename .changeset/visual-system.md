---
"family-fun-map": minor
---

Visual system: introduce elevation tokens (--elev-1/2/3) and edge-shadow tokens (--shadow-panel/--shadow-sheet) applied across floating map controls, dropdown menus, panels, and the toast; drop the one-off --shadow token. Unify the button vocabulary — attribute filter pills get a .filter-pill--attribute tier (rest tint only), the unused .action-link ghost style is removed and .action-btn is documented as icon-only. Panel headers now use the display font (Space Grotesk) like place names.
